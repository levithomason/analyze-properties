{
  "rules": {
    ".read": "auth != null",
    ".write": "auth.token.email_verified == true && auth.token.email === 'me@levithomason.com'",
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    }
  }
}
